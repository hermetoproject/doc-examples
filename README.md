# npm example

This example demonstrates using Hermeto with a simple Node.js app that uses npm for dependency management.

For a full walkthrough of how this example works, see the [npm example](https://github.com/hitshiroya/hermeto/blob/main/docs/npm.md#example) section in the Hermeto docs.

